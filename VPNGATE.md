# VPNGATE.md — 免费代理节点使用说明

七宝浏览器的「代理」是手动填写模式：选类型（HTTP / HTTPS / SOCKS5）→ 填主机 IP 和端口。
没有自己的代理？可以用 **VPN Gate**（日本筑波大学维护的公益免费 VPN 项目，公开中继服务器 5000+）
提供的公共节点，完全免费、无需注册、打开即用。本文件教你怎么拿节点、怎么填、怎么验证。

> 适用人群：没有机场订阅、不想自己搭节点、只是想偶尔翻墙看看网页的新手。
> 如果你有自己的 v2rayN / Clash / 机场订阅，直接用你自己的节点（填 HTTP 或 SOCKS5 均可），比公共节点稳得多。

---

## 〇、先记住两件事

1. **中继数量**：VPN Gate 在世界各地公开运营 **5000+ 个公共 VPN 中继服务器**，随时增减轮换。
2. **账号密码固定**：这些公共中继的认证参数是官方统一的——

   ```
   用户名: vpn
   密码:   vpn
   ```

   不管你在哪一台服务器、用哪个协议（OpenVPN / L2TP/IPsec / SSTP），都是 `vpn` / `vpn`。

---

## 一、拿到节点（三条路，任选）

### 路 A：打开网页版列表（最直观，推荐先试）

1. 在七宝浏览器「新建环境 → 代理」区点 **「去 vpngate 拿免费节点 →」**（会自动展开详细步骤并把账号 `vpn`/`vpn` 填好），
   或直接浏览器打开：

   ```
   https://www.vpngate.net/cn/
   ```

2. 页面是一张服务器表，列头可点排序：
   - 点「速度 Speed」从高到低排，选最快的
   - 优先选带绿色数字的行（在线），越靠上越快
   - 每行 IP 即服务器地址；代理端口按连接协议而定（OpenVPN 用页面给的 .ovpn 配置，
     **Socks** 列有数字的行 = 该服务器开放 SOCKS5 端口）

### 路 B：网页被墙 / 打不开时 → 从 GitHub 每日快照拉

VPN Gate 每天把全量服务器清单公开在 GitHub，不需要访问 vpngate.net：

```
https://github.com/vpngate/vpngate-csv-dump
```

- 进仓库 → 找一个最近的 `.csv` 文件 → 打开后挑在线时间长、速度快的行
- 国内访问 GitHub 慢的话，用镜像前缀替换地址开头（如 `https://ghproxy.com/` 类加速域名）

### 路 C：只想要「能翻墙」的现成出口（不折腾）

如果你对节点质量不敏感、只求能打开 Google，直接用 Cloudflare WARP 的公开 SOCKS 端点也能凑合——
不过推荐度低：速度不稳定、时灵时不灵。首选还是路 A / 路 B 的 vpngate 节点。

---

## ⚠️ 关键：先认对列，再填端口

vpngate 的列表每一行可能同时有 **多个端口**，分布在不同列：

| 列名 | 端口性质 | 浏览器能用？ |
|---|---|---|
| **Socks** | 该节点开放的 **SOCKS5 代理** 端口（如 1080/1295/1296） | ✅ **就是这个** |
| SSL-VPN | OpenConnect / SoftEther 专用（常见 TCP 443） | ❌ 需装客户端 |
| OpenVPN TCP | OpenVPN GUI 专用（常见 443/1194） | ❌ 需装客户端 |
| OpenVPN UDP | OpenVPN GUI 专用（常见 1194） | ❌ 需装客户端 |
| L2TP/IPsec | 系统 VPN 客户端专用 | ❌ 需装客户端 |
| MS-SSTP | Windows 内置 VPN 客户端专用 | ❌ 需装客户端 |

**最容易踩的坑**：看到「443」就填进浏览器代理——**443 是 OpenVPN / SSL-VPN 的端口，不是 SOCKS**。
填了之后浏览器启动 → 访问 `ip.sb` → 一定连不上（哪怕凭据对也没用）。

**正确做法**：
1. 表格里**向右滚动到「Socks」列**
2. 找该列**有数字（1080 / 1295 / 1296 / 878 等）的行**——那才是浏览器 SOCKS5 用的端口
3. 把**Socks 列里那个数字**填到「端口」
4. **不要**把 SSL-VPN / OpenVPN 列下的 443 当成端口填进来

如果一整页没有「Socks」列或所有行的 Socks 列都是空的——这页节点全都不支持浏览器代理，
往下滚换一批，或者改用路 B（GitHub 快照 CSV 里也能看到 Socks 端口字段）。

---

## 二、填进七宝浏览器

> 浏览器代理字段只吃 **HTTP/HTTPS/SOCKS5** 三种，所以这里用的是 vpngate 上
> 明确支持 **Socks** 的服务器行（无 Socks 标记的行是 OpenVPN/L2TP 类型，浏览器用不了，需装客户端）。

以找到的节点为例（假设该行 IP = `203.0.113.5`，Socks 端口 = `1080`）：

| 表单字段 | 填什么 |
|---|---|
| 类型 | `SOCKS5` |
| 主机 | `203.0.113.5`（该行的 IP） |
| 端口 | `1080`（Socks 列的端口） |
| 用户名 | `vpn`（固定） |
| 密码 | `vpn`（固定） |

**点「去 vpngate 拿免费节点 →」按钮会自动把用户名/密码填成 vpn/vpn**，不用手输。

保存 → 启动这个环境 → 在地址栏访问 `https://ip.sb` 或 `https://whoer.net`，
看到出口 IP 变成国外（如日本）就说明通了。

> 说明：vpngate 的 `vpn`/`vpn` 是**官方统一公共凭据**，不是某个服务器特有的。
> 它同时适用于 OpenVPN、L2TP/IPsec、SSTP 等协议——只不过那些协议需要安装系统级
> VPN 客户端（OpenVPN GUI / Windows 自带 VPN），七宝浏览器内嵌的代理字段只走 HTTP/SOCKS，
> 因此选节点时认准「有 Socks 端口的行」即可。

---

## 三、排错速查

| 现象 | 原因 / 处理 |
|---|---|
| **填了 443（OpenVPN 端口）** | **443 是 OpenVPN / SSL-VPN 端口，不是 SOCKS5**——回到列表找「Socks」列下那个数字填进去 |
| 填完启动，页面打不开 | 节点失效了，或端口填错。vpngate 免费节点 24 小时轮换、随时可能下线——回列表重新挑一台填 |
| 能开 Google 但很慢 | 公共节点共享带宽，正常。挑「速度」列高的，或多存几个节点轮着用 |
| 提示 407 / 认证失败 | 确认用户名密码都是 `vpn`（小写） |
| 某节点连不上（红色） | 换一台。别纠结，免费节点多的是 |
| 访问 ip.sb 仍显示本机真实 IP | 代理完全没生效。检查：① 类型选的是不是 SOCKS5 ② 端口是不是 Socks 列里的数字（不是 443）③ 用户名密码都是 `vpn` |
| 不知道哪个是「Socks 列」 | vpngate 表格列顺序大约是：国家 → DNS → IP → 速度 → Ping → SSL-VPN → L2TP → OpenVPN TCP → OpenVPN UDP → SSTP → **Socks** → 累计用户。Socks 在表格最右侧附近 |
| 表单里选不了 SOCKS5 | 下拉里有 `SOCKS5` 选项，需要先保存再启动；没看到就确认你用的是最新版 |

---

## 四、给二次开发者的话

- 代理字段 5 个控件：`f-proxy-type`（select）、`f-proxy-host`、`f-proxy-port`、
  `f-proxy-user`、`f-proxy-pass`
- 「去 vpngate」按钮点击时若用户名/密码为空会自动预填 `vpn`/`vpn`
- 主进程按 `type === 'socks5' ? 'socks5' : 'http'` 拼 `proxyRules`，交给 Chromium session
- 按钮走 IPC `meta:open-vpngate` → `shell.openExternal`，纯外链，无任何数据上报
- 七宝浏览器不内置任何节点、不代购流量、不记录你填的代理配置之外的信息——节点完全由用户自填自管
