# 七宝浏览器 · 新手使用说明

> 本文面向从没用过指纹浏览器的用户，手把手教每个空怎么填。
> 全程只需要记住一句话：**指纹浏览器 = 在你这台电脑上开一堆"互不认识的浏览器"**。

---

## 一、先搞懂它是什么

普通浏览器：你只有一个浏览器，登录 5 个淘宝号，网站很快发现"这 5 个号是同一台电脑"——因为它们共享同一套 cookie、缓存、浏览器指纹，这就是**账号关联**，轻则限流，重则封号。

指纹浏览器：把浏览器"分身"成多个独立环境。每个环境自带一套全新的：
- **身份**（浏览器型号、系统、时区、显卡、屏幕）
- **数据**（cookie、登录状态、缓存，互不相通）

对网站来说，每个环境 = 一台**从没见过的陌生电脑**。这就是多账号运营的常规操作。

**七宝浏览器**把这套能力做成本地软件，数据全部存在你电脑上，不上传。

---

## 二、怎么打开

双击应用文件夹里的 **`start.cmd`**。

（首次会等几秒，弹出一个深色窗口就是管理后台。以后启动都用它，不用管 npm 那些命令。）

---

## 三、新建环境：每个空怎么填

点击右上角 **「+ 新建环境」**，按下面三个区块填。

### 3.1 基本信息

| 空 | 怎么填 | 说明 |
|---|---|---|
| **环境名称**（必填） | 给这个浏览器分身起名 | 建议按"平台+用途"命名，如 `小红书-主号`、`淘宝-店铺A`、`测试`。方便自己一眼认出 |
| **主页** | 填你主要要用的网址 | 启动环境时自动打开的网站。默认 `https://www.baidu.com`，可改成你要用的站点（如抖音、小红书） |

> 新手记住：**只填"环境名称"，其他全部默认，就能用了。** 下面的都是"进阶但推荐了解"。

### 3.2 代理（可选，新手可以先跳过）

**什么时候需要**：多个环境共用你家的网络 IP，网站仍可能通过 IP 识别"同一个路由器"。**代理 = 给这个环境单独配一个 IP**，彻底分开。

| 空 | 怎么填 | 说明 |
|---|---|---|
| 类型 | 看你买的代理是什么协议 | 三个选项：HTTP / HTTPS / SOCKS5。买代理时商家会明确告诉你 |
| 主机 | 代理商给的 IP 或域名 | 例：`127.0.0.1`、`103.xx.xx.xx`、`proxy.example.com` |
| 端口 | 代理商给的端口号 | 例：`1080`、`8080`、`7890` |
| 用户名 / 密码 | 认证型代理才需要 | 没有账号认证就留空 |

**新手建议**：没有代理就选「不使用代理」，先用着，之后需要再加（环境可以随时编辑）。

### 3.3 指纹：伪装成什么电脑

网站通过 **UA / 时区 / 显卡 / 屏幕** 等特征给你打"身份证"。这里就是给每个环境配一套不同的身份证。

**UA 预设**（4 个选项，选一个）：

| 选项 | 适合谁 |
|---|---|
| **Windows · Chrome 126** | ✅ **新手首选**。最主流浏览器，几乎所有网站兼容性最好，最不容易穿帮 |
| Windows · Edge 126 | 想模拟微软 Edge 时用 |
| macOS · Chrome 126 | ⚠️ 高阶。选了 Mac 后，时区/WebGL 也得换成国外的，否则"苹果电脑配上海时区+英伟达显卡"会穿帮 |
| macOS · Safari 17 | ⚠️ 同上，仅高阶 |

**时区**（8 个选项：上海/香港/东京/新加坡/伦敦/柏林/纽约/洛杉矶）：
- 新手选 **「中国 · 上海」**（和你实际时区一致最自然）。
- 如果配了国外代理，选**代理所在地的时区**（如代理在美国就选纽约/洛杉矶）。

**CPU 核数**：填 **8**（默认值）就行。范围 2–64，别填太夸张，64 核的"家用电脑"反而显假。

**内存 (GB)**：填 **8**（默认值）就行，理由同上。

**WebGL 预设**（模拟的显卡，3 个选项）：
- `NVIDIA RTX 3060 (Windows)` —— 新手默认选它
- `Intel UHD 630 (Windows)` —— 想模拟集显电脑时
- `Apple M1 (macOS)` —— 只有 UA 选了 Mac 才用

> 规则：**Windows 的 UA 配 Windows 显卡，Mac 的 UA 配 M1**。混搭必穿帮。

**屏幕宽 / 屏幕高**：**留空** = 用你真实的屏幕分辨率。填了（如 `1920` / `1080`）= 伪装成该分辨率。新手留空即可。

**Canvas 噪声**：**保持勾选，永远别关**。这是防 Canvas 指纹追踪的核心手段，所有环境靠它生成不同的"画布指纹"。

**自定义 UA**（最下方文本框）：**留空**。这是高阶功能，只有你手上有一整串 `Mozilla/5.0 (Windows NT 10.0...)` 完整 UA 字符串时才填。选了「自定义 UA…」但留空会保存失败。

### 3.4 保存并启动

1. 点 **「保存」** → 回到主界面，列表出现一张环境卡片
2. 点卡片上的 **「启动」** → 弹出该环境的独立浏览器窗口，正常上网
3. 关掉这个窗口 = 停止环境；再点「启动」会带着原 cookie/登录状态回来

---

## 四、日常操作

| 操作 | 在哪 | 注意 |
|---|---|---|
| 启动 / 停止 | 卡片上的按钮 | 停止只是关窗口，数据还在 |
| 编辑 | 点卡片（或「编辑」按钮） | 改完保存，运行中的环境会自动重启生效 |
| 删除 | 卡片「删除」按钮 | ⚠️ **环境连同所有 cookie/登录状态/浏览数据一起删除，不可恢复**。删前想清楚 |
| 看数据 | 右上角「数据目录」 | 打开所有环境的浏览器数据文件夹 |

---

## 五、新手速记（3 条）

1. **只填"环境名称"就能用**，其他保持默认。
2. **一个业务线一个环境**：A 店铺、B 店铺、测试，各开一个，别混用。
3. **代理和指纹要配套**：选了国外 IP，时区/UA/显卡也换成国外的，不然"人在中国、电脑在美国、显卡是英伟达"一眼穿帮。

---

## 六、常见问题

**Q：第一次打开网站要我验证码/扫码？**
A：正常。新环境 = 网站眼里的一台新设备，首次登录验证一次，之后会记住。

**Q：网页变成英文了？**
A：macOS 那两个 UA 预设是英文环境。回到「编辑」把 UA 换成 **Windows · Chrome 126** 即可。

**Q：某个环境出问题了？**
A：直接删除重建，不影响其他环境（数据本来就完全隔离）。

**Q：为什么新手不建议选 Mac？**
A：Mac 的 UA 需要配套国外的时区、显卡、屏幕，任何一处不配套都会穿帮。Windows 预设自洽，最稳。

**Q：七宝浏览器能防封号吗？**
A：能有效防**常规的账号关联检测**（cookie、UA、时区、Canvas）。但**防不了专业反指纹**（银行、大平台风控级会检测内核级特征，指纹浏览器厂商也绕不过）。别用它做违法的事。

---

## 附：数据存在哪

- 环境配置：应用目录旁 `user-data/profiles.json`
- 各环境浏览器数据：`user-data/env-data/`（每个环境一个子目录，互不干扰）
- 全部在本机，删除应用文件夹即全部消失（记得备份 `user-data` 目录）。
